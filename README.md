# Miror Simulator

Miror Simulator is a local-first personal execution dashboard. It opens directly, stores the simulation on the device, and does not require an account or a database.

## APK releases

Push a version tag beginning with `v` to trigger the GitHub Actions Android build. The generated APK is attached to the matching GitHub release.

